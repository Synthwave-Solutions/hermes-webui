# Frontend QA findings

The tests drive real frontend controls against disposable application state. Frozen run `20260909-080212-676633000` passed all 192 browser scenarios with zero uncaught application errors in 192 collections and zero report-level errors. 13 reporter fixture-bookkeeping warning entries remain recorded separately. The final execution summary is the authority for the integrated result; historical failure descriptions below explain the fixes.

Canonical paired backend verification passed 482 WebUI tests across 34 files and 357 engine tests across 29 files at the same tested code commits. The inventory still contains 539 entries without interaction evidence out of 1,053; passing these scenarios is not exhaustive feature certification. Production deployment is authorized and pending a separate live verification result.

| Finding | Observed behavior | Disposition |
|---|---|---|
| QA-001 | Settings POST failed through a locally shadowed `save_settings` import. | Fixed; actual preference save/reload covered. |
| QA-002 | Clear conversation appeared successful, but stored context or recovery could restore old messages. | Fixed with explicit clear generation, canonical truncation and stale-context reset; new-turn/reload regression. |
| QA-003 | Terminal stream read obsolete output storage rather than the terminal subscription API. | Fixed; real shell output/restart and sequenced reconnect covered. |
| QA-004 | Successful passkey login omitted response Content-Length, leaving the browser request unfinished. | Fixed; virtual WebAuthn registration/login/removal passes. |
| QA-005 | Activity hide-all option and event-timestamp preference were rejected or silently lost. | Fixed enum/default contract; saved values and reload covered. |
| QA-006 | Auto-title refresh UI submitted a number to a string enum. | Fixed; each actual option persists. |
| QA-007 | Settings controls could accept interaction before loading bound their handlers. | Fixed with a busy/inert initialization boundary. |
| QA-008 | Passive success toasts intercepted the next click and could remain hovered indefinitely. | Fixed; passive toasts no longer capture pointer events, error actions remain usable. |
| QA-009 | Mobile Chat/Governance/Connections/My approvals navigation left a drawer over the main controls. | Fixed; normal mobile clicks and governance form roundtrip tested. |
| QA-010 | Interrupt could consume a queued replacement as a steer into the cancelled run. | Fixed; durable queue entry retries a new turn after the old worker unwinds. |
| QA-011 | Unchanged group saves dropped command deny rules and flattened command metadata. | Fixed by preserving unexposed policy data; actual fail-before/pass-after browser regression. |
| QA-012 | Committed autocomplete suggestions could overlay Save; Escape did not dismiss the menu. | Fixed shared Enter/Escape handling; normal group save clicks pass. |
| QA-013 | Duplicate scheduled job requested disabled state but was created active. | Fixed atomically at engine creation and forwarded by WebUI; copy starts paused. |
| QA-014 | The visible Share action called an unwired `/api/share/create` route and returned 404. | Fixed. Actual owner-controlled create/refresh/revoke, anonymous snapshot rendering, cancellation, later-message privacy, owner-only publishing and CSRF refusal pass. A readable group participant cannot publish or revoke the owner's snapshot. Revocation invalidates the public link without deleting the private conversation. |
| QA-015 | Kanban displayed Done for a Todo task, but completion returned 404 although the task existed. | Fixed. Explicit manual Todo/Triage completion passes with reload persistence. Pending dependencies still block completion, denied users cannot mutate tasks, and default worker eligibility and expected-run fencing remain unchanged. Invalid transitions of existing tasks return validation errors; genuinely absent tasks retain 404. |
| QA-016 | Voice treated every transcript as a chat submission and spoke a fixed forwarding acknowledgement. | Replaced with native realtime answers, trusted server tools, separate visible work chats and continued conversation; actual engine/protocol regressions, physical audio still unverified. |
| QA-017 | An idle parent with running or undelivered subagent work could be reported completed; final writeback could briefly look like a lost worker. | Status now combines session-owned live workers with exact-session delegation ledgers from the stored execution profiles; missing/corrupt/dropped-delivery state is unknown, not success. |
| QA-018 | QA provider discovery inherited the host home/PATH, found a GitHub CLI credential and attempted a Copilot exchange. | Test runner now supplies private OS home/config paths, sanitized environment and loopback/credential-discovery guards. The earlier run is historical and its broad isolation claim was retracted. Safe exports exclude credential values. |
| QA-019 | A dashboard IIFE plugin loaded blank inside an opaque sandbox because its authenticated asset redirected recursively to login. | Self-contained validated IIFE assets are embedded into the opaque page; missing-auth asset requests return401 instead of redirecting. Full SPA asset contract remains a separate limitation. |
| QA-020 | A signed-in user without plugin-read access could fetch dashboard plugin pages/assets directly. | Canonical plugin permission checks now protect page, asset and shared stylesheet entry points; browser and server negative checks added. |
| QA-021 | A user's new capability request was absent from My approvals until an administrator opened the global queue. | The authenticated requester view now ingests only that user's denial spool. Pending/approved/rejected state, audit, duplicate reads, ownership and refused self-approval are covered. |
| QA-022 | Hide new-chat welcome panel appeared to change but was silently discarded by the settings backend. | Added the missing persisted boolean contract; actual independent suggestion/panel behavior and reload regression passes. |
| QA-023 | Workspace name/path grants could bypass owner/member isolation; revocation left existing file sessions and engine work usable. | Independent live membership and governed workspace-selection ceilings now cover catalog, activation, files, uploads, chat/terminal and tool/model entry points. Private child roots and symlinks cannot borrow shared-parent access. Retained-browser and parked real-write regressions verify denial and no effect. |
| QA-024 | An asynchronous wakeup could resume with only the session owner's email, losing the initiator's authenticated group restrictions. | A trusted opaque job reference restores the original actor and hard permission ceiling, with fresh local policy/membership and the current approval policy. Missing authority or changed execution workspace fails closed. Actual signed-group continuation denial is covered. |
| QA-025 | A completed denied write could show “Updated a file” after the worker settled or the page reloaded. | Structured tool failure flags now survive live events, persisted history and card hydration; failed actions are excluded from successful-effect summaries. Actual blocked-write and successful JSON-content regressions verify the distinction. |
| QA-026 | The terminal required external CDN assets and failed inside the correctly isolated browser run. | Bundled the exact existing xterm versions with verified npm integrity, original browser SRI and MIT licenses. Actual same-origin asset responses, command execution and restart effects pass under the unchanged loopback guard. |
| QA-027 | Kanban displayed Block for a Todo task, but its transition returned 404 under the worker-only eligibility contract. | Fixed with an explicit manual UI opt-in. Actual Todo→Blocked→Unblock persists the block reason and unfinished dependency, returns the task to Todo, and records the audit event. Denied users cannot block or unblock. Repeating the same blocker preserves the configured recurrence limit and escalates to Triage; worker run fencing is retained. |

| QA-028 | A valid JSON import accepted an unauthorized private workspace that normal new-session creation rejected. The imported session was unusable: protected file and engine routes still denied access. | Import now applies the same workspace-selection guard before constructing or saving a session. Actual chooser rejection returns403 with no additional member/admin session; allowed import preserves exact transcript and current owner/default profile, ignores forged owner/profile/participants, and cannot read the private nested file. |

Security findings addressed by the RBAC implementation are documented in the separate governance package: retained denials under wildcard access, final-dispatch enforcement, one-action approval scope, stable first-event approval IDs, current-policy revalidation, delegated manual-review enforcement and concrete resource bypasses.

Global repository CI is not green. At the tested WebUI commit, seven substantive checks passed and 15 broad shards failed. The reviewed Python 3.11 shard 0 still has exactly 10 missing-contract collection errors with baseline-identical test bytes and symbols absent at both revisions. Other 14 latest failure logs were not independently re-triaged. See [exact CI qualification](evidence/ci-qualification.json); these results remain separate from the passing focused backend bundles and all 192 browser scenarios.
